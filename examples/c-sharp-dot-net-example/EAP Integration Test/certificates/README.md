This directory should contain your certificate and private key.

**Filenames**
`my-certificate.crt`: The certificate file, starts with `-----BEGIN CERTIFICATE-----`.
`my-private-key.key`: The key file, starts with `-----BEGIN PRIVATE KEY-----`.
